{
  "buildCommand": "tar -xzf lumimaid-project-new.tar.gz --strip-components=1 -C . && rm -f tsconfig.json && npm install --legacy-peer-deps && npm install --legacy-peer-deps typescript @types/node @types/react @types/react-dom && npm run build"
}
